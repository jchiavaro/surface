!!!
%html
  %head
    %meta{:charset => "utf-8"}
    %title= content_for?(:title) ? yield(:title) : "Surface"
    %meta{:content => "", :name => "description"}
    %meta{:content => "", :name => "author"}
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    = yield(:head)
  %body
    .wrapper
      %div{class: 'navbar navbar-fixed-top navbar-inverse'}
        %div.navbar-inner
          .container-fluid
            = render 'layouts/navigation'
      .container-fluid.container-fluid-home
        = render partial: 'layouts/messages', flash: flash
        = controller.sub_layout && signed_in? ? (render :partial => "/layouts/#{controller.sub_layout}") : yield
    %footer.footer
      = render 'layouts/footer'
